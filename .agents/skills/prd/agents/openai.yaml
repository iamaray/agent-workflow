interface:
  display_name: "PRD"
  short_description: "Create a PRD from a new feature brief"
  default_prompt: "Use $prd to create a new feature workspace and PRD from my feature description."

policy:
  allow_implicit_invocation: false
