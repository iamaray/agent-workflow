interface:
  display_name: "PRD Critique"
  short_description: "Critique and revise a PRD against the codebase"
  default_prompt: "Use $prd-critique to audit and revise this PRD against the repository."

policy:
  allow_implicit_invocation: false
