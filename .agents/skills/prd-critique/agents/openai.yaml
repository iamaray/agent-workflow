interface:
  display_name: "PRD Critique"
  short_description: "Infer and critique the intended feature PRD"
  default_prompt: "Use $prd-critique to infer the feature I mean and audit its PRD."

policy:
  allow_implicit_invocation: false
