interface:
  display_name: "TDD"
  short_description: "Infer the feature and design its TDD"
  default_prompt: "Use $tdd to infer the feature I mean and design its approved PRD."

policy:
  allow_implicit_invocation: false
